{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for GitCooking",
  "main": "src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "local": "./node_modules/.bin/nodemon -L --ignore public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "morgan": "^1.10.0",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
